interface:
  display_name: 'Build PCP'
  short_description: 'Build and maintain persistent project context'
  default_prompt: 'Use $build-pcp to adopt or maintain Persistent Context Protocol in this repository.'

policy:
  allow_implicit_invocation: true
